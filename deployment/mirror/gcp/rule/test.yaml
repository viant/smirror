init:
  '!gcpCredentials': $params.authWith
  gcp: ${secrets.$gcpCredentials}
  gcpProject: $gcp.ProjectID
  bucketNs: $gcp.ProjectID
  configBucket: ${bucketNs}_config
  triggerBucket: ${bucketNs}_trigger
  opsBucket: ${bucketNs}_operation
  destBucket: ${bucketNs}_mirrored

pipeline:
  setupRoute:
    action: storage:copy
    expand: true
    source:
      URL: rule.json
    dest:
      credentials: $gcpCredentials
      URL: gs://${configBucket}/StorageMirror/dataflow/test-rule.json

  trigger:
    action: storage:copy
    source:
      URL: events.csv.gz
    dest:
      credentials: $gcpCredentials
      URL: gs://${triggerBucket}/data/test/events.csv.gz
