{
  "Mirrors": {
    "BaseURL": "gs://${prefix}_config/StorageMirror/Rules",
    "CheckInMs": 60000
  },
  "SlackCredentials": {
    "URL": "gs://${prefix}_config/Secrets/slack.json.enc",
    "Key": "${prefix}_ring/${prefix}_key"
  }
}