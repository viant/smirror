pipeline:

  trigger:
    action: storage:copy
    sleepTimeMs: 20000
    source:
      URL: ${parent.path}/data/prepare/events.csv
    dest:
      credentials: $gcpCredentials
      URL: gs://${gsBucket}/data/p4/events.csv

  validate:
    chunk1:
      action: storage:download
      udf: UnzipText
      source:
        credentials: $awsCredentials
        URL: s3://${gsBucket}/data/p4/events_00001.csv.gz
      destKey: events
      expect: $Cat('${parent.path}/data/expect/events_00001.csv')
    chunk2:
      action: storage:download
      udf: UnzipText
      source:
        credentials: $awsCredentials
        URL: s3://${gsBucket}/data/p4/events_00002.csv.gz
      destKey: events
      expect: $Cat('${parent.path}/data/expect/events_00002.csv')
