init:
  part1: $Cat('${parent.path}/data/expect/events_00001.csv')
  part2: $Cat('${parent.path}/data/expect/events_00002.csv')
  events: $Cat('${parent.path}/data/expect/events.json')
  exepct: $AsData($events)

pipeline:

  setupRoute:
    action: storage:copy
    expand: true
    source:
      URL: ${parent.path}/routes.json
    dest:
      credentials: $gcpCredentials
      URL: gs://${gsConifgBucket}/StorageMirror/processes/case_${parent.index}.json

  trigger:
    action: storage:copy
    sleepTimeMs: 20000
    source:
      URL: ${parent.path}/data/prepare/events.csv
    dest:
      credentials: $gcpCredentials
      URL: gs://${gsBucket}/data/p6/events.csv

  validate:
    action: msg:pull
    timeoutMs: 10000
    source:
      URL: smirrorSubscription
      type: subscription
      vendor: gcp
      credentials: $gcpCredentials
    count: 2
    expect: $exepct
